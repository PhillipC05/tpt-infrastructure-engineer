[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
addopts = -v --cov=backend --cov-report=term-missing --cov-report=html:htmlcov
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning